PATH=$PATH:$HOME/bin
PATH=$PATH:$HOME/bin/excluded
PATH=$PATH:$HOME/bin/remotecopy

export PATH

function vipath() {
    TMP=`mktemp -t tmp.XXXXXXXXX`;
    editpath $1 2> $TMP;
    if [ "$?" -ne 0 ]; then
        cat $TMP;
    else
        source $TMP
    fi
    rm -f $TMP
}
