#!/usr/bin/env bb

(ns ctail
  (:require [babashka.process :as p]))

(when (= *file* (System/getProperty "babashka.file"))
  (println (read-line))
  (System/exit (:exit (apply p/shell {:in *in* :continue true} "tail" *command-line-args*))))

; vim: set ft=clojure:
