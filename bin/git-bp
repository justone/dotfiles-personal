#!/bin/bash

first="${1:-master}"
second="${2:-HEAD}"

common=$(diff -u <(git rev-list --first-parent "$first") <(git rev-list --first-parent "$second") | sed -ne 's/^ //p' | head -n 1)

git lg $common^..$first $common^..$second
