#!/usr/bin/env bb

(ns crg
  (:require [babashka.process :as p]))

(when (= *file* (System/getProperty "babashka.file"))
  (println (read-line))
  (apply p/shell {:in *in*} "rg" *command-line-args*))

; vim: set ft=clojure:
